# allocate_cities

Given a destination, duration, pace, interests, and date window, propose a city set sized to the trip.

Output: JSON array of city candidates with nights, coordinates, and concise reasoning.
